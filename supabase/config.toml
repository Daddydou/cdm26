# project id : ubnkuwyqclrjckogldlc
# (section [project] retirée — conflit avec le CLI Supabase)

[functions.lock-picks]
verify_jwt = false
# Cron : toutes les minutes
# À configurer dans Dashboard → Edge Functions → lock-picks → Schedules
# Schedule : * * * * *

[functions.match-reminder]
verify_jwt = false
# Cron : toutes les heures
# À configurer dans Dashboard → Edge Functions → match-reminder → Schedules
# Schedule : 0 * * * *
